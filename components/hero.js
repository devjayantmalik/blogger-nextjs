import Head from "next/head";

const Hero = () => {
    return (
        <div className="jumbotron text-center">
            <h2>Blogging Platform</h2>
            <p>The most amazing blogging platform on the web.</p>
        </div>
    );
};

export default Hero;
